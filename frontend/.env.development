# Development Environment Configuration
# This file is used when NODE_ENV is not set to "production"

# Development mode - uses mock authentication
NODE_ENV=development

# Mock AWS configuration (not used in dev mode, but prevents console warnings)
VITE_AWS_REGION=us-east-1
VITE_AWS_USER_POOL_ID=us-east-1_mockpool
VITE_AWS_USER_POOL_CLIENT_ID=mockclient123
VITE_API_GATEWAY_URL=http://localhost:8080

# Backend URL for development
VITE_BACKEND_URL=http://localhost:8080
